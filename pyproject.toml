[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "wp2aws"
version = "0.1.0"
description = "WordPress-to-AWS migration sizing tool"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
